import { initializeApp,getApps,getApp } from 'https://www.gstatic.com/firebasejs/10.8.1/firebase-app.js';import { getFirestore,collection,query,where,getDocs } from 'https://www.gstatic.com/firebasejs/10.8.1/firebase-firestore.js';const cfg={apiKey:'AIzaSyAw_TmBCgvAwFPZkJ8RQ7_jSFB62KgQVqc',authDomain:'selena-events-dashboard.firebaseapp.com',projectId:'selena-events-dashboard',storageBucket:'selena-events-dashboard.firebasestorage.app',messagingSenderId:'535842804912',appId:'1:535842804912:web:7464adf97bcdf456131775'};const app=getApps().length?getApp():initializeApp(cfg),db=getFirestore(app),esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const snap=await getDocs(query(collection(db,'team_members'),where('visible','==',true)));if(!snap.empty){const members=snap.docs.map(d=>({id:d.id,...d.data()})).sort((a,b)=>Number(a.order||0)-Number(b.order||0));document.getElementById('team-grid').innerHTML=members.map(m=>`<article class="text-center stagger-item group"><div class="relative w-48 h-48 mx-auto mb-6 overflow-hidden rounded-full border-4 border-white shadow-lg group-hover:shadow-2xl transition-all"><img src="${esc(m.image)}" alt="${esc(m.name)}" class="w-full h-full object-cover transform group-hover:scale-110 transition-all duration-700"></div><h2 class="text-xl font-serif mb-2 text-dark">${esc(m.name)}</h2><h3 class="text-xs font-semibold tracking-widest text-gold uppercase mb-4">${esc(m.role)}</h3><p class="text-gray-500 font-light text-sm leading-relaxed">${esc(m.description)}</p></article>`).join('');}
