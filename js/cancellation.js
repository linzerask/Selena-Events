import { initializeApp,getApps,getApp } from 'https://www.gstatic.com/firebasejs/10.8.1/firebase-app.js';
import { getAuth,onAuthStateChanged } from 'https://www.gstatic.com/firebasejs/10.8.1/firebase-auth.js';
import { getFirestore,collection,addDoc,serverTimestamp } from 'https://www.gstatic.com/firebasejs/10.8.1/firebase-firestore.js';
const cfg={apiKey:'AIzaSyAw_TmBCgvAwFPZkJ8RQ7_jSFB62KgQVqc',authDomain:'selena-events-dashboard.firebaseapp.com',projectId:'selena-events-dashboard',storageBucket:'selena-events-dashboard.firebasestorage.app',messagingSenderId:'535842804912',appId:'1:535842804912:web:7464adf97bcdf456131775'};
const app=getApps().length?getApp():initializeApp(cfg),auth=getAuth(app),db=getFirestore(app);let currentUser=null;
onAuthStateChanged(auth,u=>{currentUser=u;document.querySelectorAll('#cancellation-login-note').forEach(x=>x.remove());if(!u&&document.getElementById('cancellation-form')){const p=document.createElement('p');p.id='cancellation-login-note';p.className='text-sm text-red-600';p.innerHTML='Bitte zuerst <a class="underline" href="login.html">einloggen</a>.';document.getElementById('cancellation-form').prepend(p);}});
document.getElementById('cancellation-form')?.addEventListener('submit',async e=>{e.preventDefault();const status=document.getElementById('cancellation-status');if(!currentUser){status.textContent='Bitte zuerst einloggen.';return;}const eventDate=document.getElementById('cancel-event-date').value;const cutoff=new Date(eventDate+'T12:00:00');cutoff.setDate(cutoff.getDate()-14);if(new Date()>cutoff){status.textContent='Online-Stornierungsanfragen über diese vertragliche Option sind nur bis 14 Kalendertage vor dem Event möglich. Bitte kontaktieren Sie uns direkt; gesetzliche Rechte bleiben unberührt.';status.className='text-sm text-red-600';return;}const data={customerUid:currentUser.uid,customerEmail:currentUser.email||'',orderId:document.getElementById('cancel-order-id').value.trim(),eventDate,reason:document.getElementById('cancel-reason').value.trim(),status:'Neu',createdAt:serverTimestamp()};const request=await addDoc(collection(db,'cancellation_requests'),data);e.target.reset();status.textContent=`Anfrage ${request.id} wurde gesendet. Sie wird erst nach Bestätigung durch Selena Events wirksam.`;status.className='text-sm text-green-600';});
