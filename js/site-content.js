import { initializeApp,getApps,getApp } from 'https://www.gstatic.com/firebasejs/10.8.1/firebase-app.js';import { getFirestore,doc,getDoc } from 'https://www.gstatic.com/firebasejs/10.8.1/firebase-firestore.js';const cfg={apiKey:'AIzaSyAw_TmBCgvAwFPZkJ8RQ7_jSFB62KgQVqc',authDomain:'selena-events-dashboard.firebaseapp.com',projectId:'selena-events-dashboard',storageBucket:'selena-events-dashboard.firebasestorage.app',messagingSenderId:'535842804912',appId:'1:535842804912:web:7464adf97bcdf456131775'};const app=getApps().length?getApp():initializeApp(cfg),db=getFirestore(app);const snap=await getDoc(doc(db,'settings','site_images'));if(snap.exists()){const s=snap.data();document.querySelectorAll('.hero-slide img').forEach((img,i)=>{if(s[`hero${i+1}`])img.src=s[`hero${i+1}`]});document.querySelectorAll('#highlight-slider img').forEach((img,i)=>{if(s[`highlight${i+1}`])img.src=s[`highlight${i+1}`]});}const media=await getDoc(doc(db,'settings','media_overrides'));if(media.exists()){const items=media.data().items||{};document.querySelectorAll('img[src]').forEach(img=>{const raw=img.getAttribute('src').replace(/^\.\//,'').replace(/^\.\.\//,'');for(const [path,url] of Object.entries(items)){if(raw===path||raw.endsWith('/'+path)||decodeURI(new URL(img.src,location.href).pathname).endsWith('/'+path)){img.src=url;break;}}});}
